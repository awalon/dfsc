#[cfg(test)]
mod tests {
    //use super::*;

    #[test]
    fn new_file() {
        /*        let root_folder = "examples";
                let contents = "\
        Rust:
        safe, fast, productive.
        Pick three.";
        */
        //assert_eq!(vec!["safe, fast, productive."], search(query, contents));
    }
}
